# AGENTS.md

1. 默认使用中文回复用户问题。
2. 如果网络出现问题，使用代理 `127.0.0.1:7897` 重试；加代理后执行一样的命令不需要重复询问权限。
3. 如果新建会话里要修改工作区文件，但工作区有文件未提交，可以打断并询问是否要先提交或清理工作区。
4. 如果可能，尽量使用 subagent 提高效率。
5. 线上日志、部署、服务状态、DJ 自动选歌等运维排障，必须先查被 Git 忽略的 `.local/ops/production-runbook.md`，再执行命令；不要只凭记忆、聊天记录或 AGENTS 摘要操作。该文件不存在或信息过期时停止线上操作并请环境所有者恢复。`docs/ops-runbook.md` 只说明公开/私密边界，不包含真实生产参数。
