# Superpowers 工作档案

这个目录保存历史设计、规格和实施计划，用来追溯决策背景。这里的旧文档可能包含当时的 WebSocket 架构、未勾选的实施步骤或已经被后续实现替代的文件路径。

当前项目事实以以下文件为准：

- `README.md`：面向人类读者的当前能力与运行方式
- `CLAUDE.md`：面向项目内 AI agent 的当前架构、路由和约定
- `docs/ops-runbook.md`：生产运维信息的公开/本地隐私边界

如果历史计划已经落地，只在计划顶部补状态说明；不要为了“追平代码”大面积重写旧方案正文，否则会丢失当时的设计语境。
